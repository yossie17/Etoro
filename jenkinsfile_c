def FIRST_JOB_NAME = env.FIRST_JOB_NAME
pipeline {
    agent any 
    
    stages {
        stage('Build number of the first job') { 
            steps { 
                script{
                    def jobname = "Etoro"
                    def job = Jenkins.instance.getItemByFullName(jobname)
                    def build = job.getLastBuild()
                    print(build)

                }

            }
        }
       
        
    }
}