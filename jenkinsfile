pipeline {
    agent any 
    
    stages {
        stage('Job Name Script') { 
            steps {
                sh 'echo $BUILD_NUMBER && echo $JOB_NAME' 
                sh 'python3 job_name.py' 
            }
        }
       
        
    }
}
